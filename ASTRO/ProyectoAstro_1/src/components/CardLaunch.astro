---
interface Props {
    //Tipos que hay
    id: string
    name: string
    img: string
    success: boolean
    flightNumber: number
    details: string
}

const {success, flightNumber, img, id, details }= Astro.props
const sucessText = success ? "EXITO" : "FRACASO"
---
<a href="{'/lauch/${id}'}" class="rounded-lg border shadow-md bg-gray-800 border-x-gray-700 hover:scale-105 hover:bg-gray-700 hover:bg-gray-500 transition flex flex-col ">
    <picture class="flex justify-center p-4">
        <img src="{img}" class="mb-5 rounded-lg" alt="{'Patch for launch ${id}">
    </picture>
</a>